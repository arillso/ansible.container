---
# K3s service handlers

- name: Reload systemd
  ansible.builtin.systemd:
      daemon_reload: true
  become: true

- name: Restart k3s
  ansible.builtin.systemd:
      name: k3s
      state: restarted
  become: true
